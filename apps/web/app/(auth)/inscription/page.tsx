export default function SignUpPage() {
  return <main className="shell"><div className="card" style={{maxWidth:620,margin:"48px auto"}}><span className="brand">IDEL OS</span><h2 style={{marginTop:32}}>Créer votre espace</h2><p>Un cabinet individuel sera créé automatiquement. Vous activerez ensuite le TOTP obligatoire.</p><form><label className="field">Prénom<input name="firstName" autoComplete="given-name" required /></label><label className="field">Nom<input name="lastName" autoComplete="family-name" required /></label><label className="field">Numéro RPPS<input name="rpps" inputMode="numeric" pattern="[0-9]{11}" maxLength={11} required /></label><label className="field">Adresse e-mail<input type="email" autoComplete="email" required /></label><label className="field">Mot de passe<input type="password" autoComplete="new-password" minLength={12} required /></label><button className="button" type="submit" style={{width:"100%"}}>Créer mon espace sécurisé</button></form></div></main>;
}
